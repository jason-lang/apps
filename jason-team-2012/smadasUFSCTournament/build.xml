<?xml version="1.0" encoding="ISO-8859-1" ?>

<!--

                    This file was generated by Jason 1.3.7
                    http://jason.sf.net
                    
                    May 17, 2012 - 07:54:41
-->

<project name   ="smadas2"
         basedir="."
         default="runB">

    <property name="mas2j.project.file" value="smadas2B.mas2j"/>
    <property name="debug" value=""/> <!-- use "-debug" to run in debug mode -->
    <property name="build.dir" value="${basedir}/bin/classes" />

    <property environment="env" />
    <property name="distDir" value="${env.HOME}/x_x/MAPC-UFSCTeam2012" />
    <property name="distFile" value="${env.HOME}/MAPC-UFSCTeam2012" />

    <property name="jasonJar" value="lib/jason.jar"/>

    <path id="project.classpath">
        <pathelement location="${basedir}"/>
        <pathelement location="${build.dir}"/>
        <pathelement location="${jasonJar}"/>
        <fileset dir="${basedir}/lib" >  <include name="*.jar" /> </fileset>

    </path>

    <target name="init">
        <mkdir dir="${build.dir}" />
    </target>

    <target name="compile" depends="init">
        <condition property="srcdir" value="${basedir}/src/java" else="${basedir}" >
            <available file="${basedir}/src/java" />
        </condition>
        <javac srcdir="${srcdir}" destdir="${build.dir}" debug="true" optimize="true" includeantruntime="false" >
            <classpath refid="project.classpath"/>
        </javac>
    </target>
    
    <target name="runA" depends="compile" >
        <echo message="Running project ${ant.project.name}" />
        <java classname="jason.infra.centralised.RunCentralisedMAS"
              failonerror="true" fork="yes" dir="${basedir}" >
            <classpath refid="project.classpath"/>
            <arg line="smadas2A.mas2j ${debug} "/>
            <!-- jvmarg line="-Xmx500M -Xss8M"/ -->    
        </java>
    </target>

    <target name="runB" depends="compile" >
        <echo message="Running project ${ant.project.name}" />
        <java classname="jason.infra.centralised.RunCentralisedMAS"
              failonerror="true" fork="yes" dir="${basedir}" >
            <classpath refid="project.classpath"/>
            <arg line="smadas2B.mas2j ${debug} "/>
            <!-- jvmarg line="-Xmx500M -Xss8M"/ -->    
        </java>
    </target>

    <target name="clean" >
        <delete failonerror="no" includeEmptyDirs="true" verbose="true">
            <fileset dir="${basedir}" includes="**/*.class"/>
        </delete>
    </target>

    <target name="dist" >

        <echo message="Preparing files for distribution" />

        <fixcrlf eol="crlf" includes="**/*.txt,**/*.bat,**/*.mas2j,**/*.asl" srcdir="." />

        <fixcrlf tab="remove" tablength="4" javafiles="true" includes="**/*.java,**/*.xml,**/*.mas2j,**/*.asl" srcdir="." />

        <delete failonerror="no" includeEmptyDirs="true" verbose="true">
            <fileset defaultexcludes="no" dir="." includes="**/*~" />
            <fileset dir="${basedir}" includes="**/*.class" />
            <fileset dir="${basedir}" includes="**/*.backup" />
            <fileset dir="${basedir}" includes="*.old" />
            <fileset dir="${basedir}" includes="*.log" />
            <fileset dir="${basedir}" includes="**/.err" />
        </delete>

        <delete failonerror="no" includeEmptyDirs="true">
            <fileset dir="${distDir}" />
        </delete>
        <delete dir="${distDir}/.." />
        <mkdir dir="${distDir}" />

        <copy todir="${distDir}">
            <fileset dir="${basedir}" includes="*.*" />
            <fileset dir="${basedir}" includes="lib/**/*" />
            <fileset dir="${basedir}" includes="src/**/*" />
        </copy>

        <!--delete failonerror="no" includeEmptyDirs="true">
            <fileset dir="${distDir}" includes=".settings" />
            <fileset dir="${distDir}" includes=".project" />
            <fileset dir="${distDir}" includes=".classpath" />
        </delete -->
        <delete dir="${distDir}/.svn" />

    <zip basedir="${distDir}/.." includes="**/*" zipfile="${distFile}.zip"/>
    <delete dir="${env.HOME}/x_x/" />
    </target>


</project>
