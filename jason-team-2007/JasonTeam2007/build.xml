<?xml version="1.0" encoding="ISO-8859-1" ?>

<!--

                    This file was generated by Jason 0.9.5c
                    http://jason.sf.net
                    
                    May 06, 2007 - 20:10:51
-->

<project name   ="miners"
         basedir="."
         default="run">

    <property name="mas2j.project.file" value="Local-JasonTeam.mas2j"/>
    <property name="debug" value=""/> <!-- use "-debug" to run in debug mode -->

    <property name="jasonJar" value="/Users/jomi/jason/lib/jason.jar"/>

    <path id="project.classpath">
        <pathelement location="."/>
        <pathelement location="${jasonJar}"/>
        <fileset dir="${basedir}/lib" >  <include name="*.jar" /> </fileset>

    </path>

    <target name="compile" >
        <javac srcdir="${basedir}" destdir="${basedir}" debug="true" optimize="true" >
            <classpath refid="project.classpath"/>
        </javac>
    </target>
	
    <target name="jar" depends="compile">
        <delete file="${ant.project.name}.jar" />
        <jar jarfile="${ant.project.name}.jar">
            <fileset dir="${basedir}">
                 <include name="**/*" />
                 <exclude name="lib/**" />
            </fileset>
        </jar>
    </target>

    <target name="run" depends="compile" >
        <echo message="Running project ${ant.project.name}" />
        <java classname="jason.infra.centralised.RunCentralisedMAS"
              failonerror="true" fork="yes" dir="${basedir}" >
            <classpath refid="project.classpath"/>
            <arg line="${mas2j.project.file} ${debug}"/>
        </java>
    </target>

    <target name="clean" >
        <delete failonerror="no" includeEmptyDirs="true" verbose="true">
            <fileset dir="${basedir}" includes="**/*.class"/>
        </delete>
    </target>



</project>
