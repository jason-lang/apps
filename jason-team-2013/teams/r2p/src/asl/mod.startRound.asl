+edges(E): true <-
    +edges(E);
    .print("Edges ", E).
    
+vertices(V): true <-
    +vertices(V);
    .print("Vertices ", V).
    
+steps(S): true <-
    +steps(S);
    .print("Steps ", S).
    
+simStart:
    true
<-
    .print("Contest started!").
    